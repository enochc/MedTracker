<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.MedTracker" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Transparent activity theme for dialog-only screens (e.g. widget take medication) -->
    <style name="Theme.MedTracker.Transparent" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
    </style>
</resources>
